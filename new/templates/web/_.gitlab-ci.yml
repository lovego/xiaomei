stages:
  - deploy

deploy:
  stage: deploy
  tags:
    - shell
  script:
    - xiaomei web deploy $CI_ENVIRONMENT_NAME --wait=false
  environment:
    name: $CI_COMMIT_REF_NAME
    url: https://{{ .ProNameUrlSafe }}.$CI_COMMIT_REF_NAME.{{ .Domain }}
  only:
    refs:
      - branches
    variables:
      - $CI_COMMIT_REF_NAME == 'qa'
      - $CI_COMMIT_REF_NAME == 'qa2'
      - $CI_COMMIT_REF_NAME == 'uat'

deploy-production:
  stage: deploy
  tags:
    - shell
  script:
    - xiaomei web deploy production --wait=false
  environment:
    name: production
    url: https://{{ .ProNameUrlSafe }}.{{ .Domain }}
  only:
    refs:
      - branches
    variables:
      - $CI_COMMIT_REF_NAME == 'master'
  when: manual

