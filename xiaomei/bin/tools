#!/bin/bash
cd $(dirname $0)/..

bin=$(pwd)/release/bin
GOBIN=$bin go install ./tools || exit 1

./bin/spec || exit 1

if test "$1" == qa -o "$1" == production; then
  GOENV=$1 release/bin/tools "${@:2}"
else
  release/bin/tools "${@:1}"
fi
