#!/bin/bash

cd $(dirname $0)/..
bin=$(pwd)/release/bin
GOBIN=$bin go install ./tools     || exit 1
GOBIN=$bin go install ./appserver || exit 1

./bin/spec || exit 1

./release/bin/tools assets

git status

